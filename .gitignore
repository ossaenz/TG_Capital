node_modules/
.env
*.env
*.key
*.pem
cs_tracker.json
cs_etf_tracker.json
*.secret
dist/
.DS_Store
/.idea
/.vscode
/.config.json
exported_data/
config.json

# Local Schwab companion — keep tokens, certs, and runtime data out of git
local/.schwab-tokens.json
local/.ssl-cert.pem
local/.ssl-key.pem
local/.schwab-account-hash.json
local/data/
local/certs/
local/*.csv

# Schwab SQLite bridge (local/schwab-bridge/)
local/schwab-bridge/vault.db
local/schwab-bridge/vault-data/
local/schwab-bridge/.venv/
local/schwab-bridge/__pycache__/
local/schwab-bridge/.env

# Python bytecode cache anywhere under local/ (sec_scraper.py, schwab-bridge, etc.)
**/__pycache__/
*.pyc

# Personal reference material — not app code, not meant for distribution
local/ebooks/

# Claude Code's own local tool config/session state — machine- and user-specific,
# irrelevant to anyone else cloning this repo
.claude/
local/.claude/
local/
